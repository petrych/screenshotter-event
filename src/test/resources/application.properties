storage.location=test-storage

logging.level.org.springframework.jdbc.datasource.init.ScriptUtils=DEBUG

### ========= DATABASE: TEST

spring.datasource.url=jdbc:mysql://localhost:3306/testdb
spring.datasource.username=testuser
spring.datasource.password=123

### 'none' disables Hibernate automatic schema creation
### 'create' first drops existing tables and then creates new tables
spring.jpa.hibernate.ddl-auto=create

### 'always' initialize a database using scripts
spring.sql.init.mode=always

### ========= DATABASE: GOOGLE CLOUD PLATFORM

spring.cloud.gcp.sql.enabled=false

### ========= GOOGLE CLOUD PLATFORM

app.bucket-for-images=${sm://projects/148500988272/secrets/test-storage-location}